# QuadroBoBR - Админ-панель

Упрощенная система управления контентом для сайта проката квадроциклов.

## 🚀 Быстрый старт

### 1. Установка зависимостей (уже выполнено):

```bash
cd admin
npm install
```

### 2. Запуск dev сервера:

```bash
npm run dev
```

Откроется на http://localhost:5173

---

## 🔑 Вход в систему

```
Email: admin@quadrobobr.ru
Пароль: admin123
```

---

## 📁 Структура

```
admin/
├── src/
│   ├── pages/
│   │   ├── LoginPage.tsx    # Страница авторизации
│   │   └── AdminPage.tsx    # Защищенная админ-страница
│   ├── utils/
│   │   └── auth.ts          # Утилиты для работы с JWT
│   ├── App.tsx              # Главный компонент с роутингом
│   ├── main.tsx             # Точка входа
│   └── index.css            # Базовые стили
├── index.html
├── vite.config.ts           # Конфигурация Vite
└── package.json
```

---

## 🛠️ Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **React Router 6** - роутинг
- **Vite 5** - сборщик и dev server

---

## 🔐 Авторизация

### Как это работает:

1. Пользователь вводит email/пароль на странице `/login`
2. Отправляется запрос на `POST /api/auth/login`
3. Backend возвращает JWT токен
4. Токен сохраняется в localStorage
5. Пользователь перенаправляется на `/admin`
6. Защищенные роуты проверяют наличие токена

### API:

```typescript
// Вход
POST /api/auth/login
Body: { email: string, password: string }
Response: { access_token: string, admin: {...} }
```

---

## 🎨 Следующие шаги

Сейчас реализовано:
- ✅ Авторизация
- ✅ Пустая админ-страница

Будут добавлены постепенно:
- ⏳ Управление слайдерами
- ⏳ Управление видео
- ⏳ Управление галереей
- ⏳ Управление отзывами
- ⏳ Управление маршрутами
- ⏳ Формы обратной связи
- ⏳ Редактирование текстов

---

## 📚 Полезные команды

```bash
# Разработка
npm run dev

# Production build
npm run build

# Предпросмотр production сборки
npm run preview
```

---

**Готово к разработке!** 🚀

