# 📁 Структура админ-панели QuadroBoBR

## Архитектура

Проект следует модульной архитектуре с разделением на:
- **UI Kit** - переиспользуемые компоненты дизайн-системы
- **Pages** - страницы приложения (каждая в своей папке)
- **Utils** - вспомогательные функции

---

## 🎨 UI Kit

Все переиспользуемые UI компоненты находятся в `src/ui-kit/`:

```
src/ui-kit/
├── Button/
│   ├── Button.tsx             # Компонент
│   ├── Button.module.scss     # Стили
│   └── index.ts               # Экспорт
│
├── Input/
│   ├── Input.tsx
│   ├── Input.module.scss
│   └── index.ts
│
└── index.ts                   # Центральный экспорт
```

### Использование:

```tsx
import { Button, Input } from '../../ui-kit';

<Button variant="primary">Войти</Button>
<Button variant="danger">Выйти</Button>

<Input label="Email" type="email" />
<Input label="Пароль" type="password" error="Ошибка" />
```

---

## 📄 Pages (Страницы)

Каждая страница - отдельная папка с компонентом и стилями:

```
src/pages/
├── LoginPage/
│   ├── LoginPage.tsx
│   ├── LoginPage.module.scss
│   └── index.ts
│
└── AdminPage/
    ├── AdminPage.tsx
    ├── AdminPage.module.scss
    └── index.ts
```

### Правила:
- ✅ Компонент называется с заглавной буквы
- ✅ SCSS модуль: `ComponentName.module.scss`
- ✅ Экспорт через `index.ts`
- ✅ Импорт: `import LoginPage from './pages/LoginPage'`

---

## 🎨 SCSS Модули

### Правила именования:

```scss
// ComponentName.module.scss

.container {
  // Стили контейнера
}

.title {
  // Стили заголовка
}

// Вложенность для состояний:
.button {
  &:hover {
    // hover стили
  }
  
  &.active {
    // active стили
  }
}
```

### Использование:

```tsx
import styles from './ComponentName.module.scss';

<div className={styles.container}>
  <h1 className={styles.title}>Заголовок</h1>
</div>
```

---

## 🛠️ Добавление нового компонента в UI Kit

### 1. Создать папку:
```bash
mkdir -p src/ui-kit/NewComponent
```

### 2. Создать файлы:
```
NewComponent/
├── NewComponent.tsx
├── NewComponent.module.scss
└── index.ts
```

### 3. Добавить экспорт в `src/ui-kit/index.ts`:
```typescript
export { default as NewComponent } from './NewComponent';
```

---

## 📄 Добавление новой страницы

### 1. Создать папку:
```bash
mkdir -p src/pages/NewPage
```

### 2. Создать файлы:
```
NewPage/
├── NewPage.tsx
├── NewPage.module.scss
└── index.ts
```

### 3. Добавить роут в `App.tsx`:
```typescript
import NewPage from './pages/NewPage';

<Route path="/new" element={<NewPage />} />
```

---

## 🎯 Принципы

1. **Модульность** - каждый компонент в своей папке
2. **SCSS модули** - нет глобальных стилей (кроме base)
3. **UI Kit** - переиспользуемые компоненты
4. **TypeScript** - строгая типизация
5. **Чистота** - нет inline стилей

---

## 📚 Технологии

- **React 18** + TypeScript
- **SCSS Modules** - изолированные стили
- **Vite 5** - сборщик
- **React Router 6** - роутинг

---

**Готово к разработке!** 🚀

